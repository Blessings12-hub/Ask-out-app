Add one song here, named exactly:

song.mp3

Notes:
- Pick a song that means something to the two of you — that's the whole point.
- I can't include a real song file myself: copyrighted music can't be
  reproduced or distributed this way, and I have no way to generate audio.
  You'll need to add the file yourself.
- Keep it reasonably sized (a few MB) so the page loads quickly, especially
  if you're sharing the link over mobile data.
- Browsers block audio from autoplaying without a tap, which is why the
  music only starts once she taps "Okay, go on" on the first slide —
  that's expected, not a bug.
